

public class tableObject{
	
	tableObject(String iden, Operation typeNew, String val) {//placeholder object, value should be something other than a string. Possibly an operand?
		String identifier = iden;
		Operation type = typeNew; 
		String value = val;
	}
}
